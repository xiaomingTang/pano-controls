(function webpackUniversalModuleDefinition(root, factory) {
	if(typeof exports === 'object' && typeof module === 'object')
		module.exports = factory(require("three"));
	else if(typeof define === 'function' && define.amd)
		define(["three"], factory);
	else if(typeof exports === 'object')
		exports["PanoControls"] = factory(require("three"));
	else
		root["PanoControls"] = factory(root["THREE"]);
})(window, function(__WEBPACK_EXTERNAL_MODULE__0__) {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 2);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports) {

module.exports = __WEBPACK_EXTERNAL_MODULE__0__;

/***/ }),
/* 1 */
/***/ (function(module, exports, __webpack_require__) {

(function webpackUniversalModuleDefinition(root, factory) {
	if(true)
		module.exports = factory();
	else { var i, a; }
})(window, function() {
return /******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 1);
/******/ })
/************************************************************************/
/******/ ([
/* 0 */
/***/ (function(module, exports, __webpack_require__) {

var __WEBPACK_AMD_DEFINE_RESULT__;/*!
 * EventEmitter v5.2.8 - git.io/ee
 * Unlicense - http://unlicense.org/
 * Oliver Caldwell - https://oli.me.uk/
 * @preserve
 */

;(function (exports) {
    'use strict';

    /**
     * Class for managing events.
     * Can be extended to provide event functionality in other classes.
     *
     * @class EventEmitter Manages event registering and emitting.
     */
    function EventEmitter() {}

    // Shortcuts to improve speed and size
    var proto = EventEmitter.prototype;
    var originalGlobalValue = exports.EventEmitter;

    /**
     * Finds the index of the listener for the event in its storage array.
     *
     * @param {Function[]} listeners Array of listeners to search through.
     * @param {Function} listener Method to look for.
     * @return {Number} Index of the specified listener, -1 if not found
     * @api private
     */
    function indexOfListener(listeners, listener) {
        var i = listeners.length;
        while (i--) {
            if (listeners[i].listener === listener) {
                return i;
            }
        }

        return -1;
    }

    /**
     * Alias a method while keeping the context correct, to allow for overwriting of target method.
     *
     * @param {String} name The name of the target method.
     * @return {Function} The aliased method
     * @api private
     */
    function alias(name) {
        return function aliasClosure() {
            return this[name].apply(this, arguments);
        };
    }

    /**
     * Returns the listener array for the specified event.
     * Will initialise the event object and listener arrays if required.
     * Will return an object if you use a regex search. The object contains keys for each matched event. So /ba[rz]/ might return an object containing bar and baz. But only if you have either defined them with defineEvent or added some listeners to them.
     * Each property in the object response is an array of listener functions.
     *
     * @param {String|RegExp} evt Name of the event to return the listeners from.
     * @return {Function[]|Object} All listener functions for the event.
     */
    proto.getListeners = function getListeners(evt) {
        var events = this._getEvents();
        var response;
        var key;

        // Return a concatenated array of all matching events if
        // the selector is a regular expression.
        if (evt instanceof RegExp) {
            response = {};
            for (key in events) {
                if (events.hasOwnProperty(key) && evt.test(key)) {
                    response[key] = events[key];
                }
            }
        }
        else {
            response = events[evt] || (events[evt] = []);
        }

        return response;
    };

    /**
     * Takes a list of listener objects and flattens it into a list of listener functions.
     *
     * @param {Object[]} listeners Raw listener objects.
     * @return {Function[]} Just the listener functions.
     */
    proto.flattenListeners = function flattenListeners(listeners) {
        var flatListeners = [];
        var i;

        for (i = 0; i < listeners.length; i += 1) {
            flatListeners.push(listeners[i].listener);
        }

        return flatListeners;
    };

    /**
     * Fetches the requested listeners via getListeners but will always return the results inside an object. This is mainly for internal use but others may find it useful.
     *
     * @param {String|RegExp} evt Name of the event to return the listeners from.
     * @return {Object} All listener functions for an event in an object.
     */
    proto.getListenersAsObject = function getListenersAsObject(evt) {
        var listeners = this.getListeners(evt);
        var response;

        if (listeners instanceof Array) {
            response = {};
            response[evt] = listeners;
        }

        return response || listeners;
    };

    function isValidListener (listener) {
        if (typeof listener === 'function' || listener instanceof RegExp) {
            return true
        } else if (listener && typeof listener === 'object') {
            return isValidListener(listener.listener)
        } else {
            return false
        }
    }

    /**
     * Adds a listener function to the specified event.
     * The listener will not be added if it is a duplicate.
     * If the listener returns true then it will be removed after it is called.
     * If you pass a regular expression as the event name then the listener will be added to all events that match it.
     *
     * @param {String|RegExp} evt Name of the event to attach the listener to.
     * @param {Function} listener Method to be called when the event is emitted. If the function returns true then it will be removed after calling.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.addListener = function addListener(evt, listener) {
        if (!isValidListener(listener)) {
            throw new TypeError('listener must be a function');
        }

        var listeners = this.getListenersAsObject(evt);
        var listenerIsWrapped = typeof listener === 'object';
        var key;

        for (key in listeners) {
            if (listeners.hasOwnProperty(key) && indexOfListener(listeners[key], listener) === -1) {
                listeners[key].push(listenerIsWrapped ? listener : {
                    listener: listener,
                    once: false
                });
            }
        }

        return this;
    };

    /**
     * Alias of addListener
     */
    proto.on = alias('addListener');

    /**
     * Semi-alias of addListener. It will add a listener that will be
     * automatically removed after its first execution.
     *
     * @param {String|RegExp} evt Name of the event to attach the listener to.
     * @param {Function} listener Method to be called when the event is emitted. If the function returns true then it will be removed after calling.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.addOnceListener = function addOnceListener(evt, listener) {
        return this.addListener(evt, {
            listener: listener,
            once: true
        });
    };

    /**
     * Alias of addOnceListener.
     */
    proto.once = alias('addOnceListener');

    /**
     * Defines an event name. This is required if you want to use a regex to add a listener to multiple events at once. If you don't do this then how do you expect it to know what event to add to? Should it just add to every possible match for a regex? No. That is scary and bad.
     * You need to tell it what event names should be matched by a regex.
     *
     * @param {String} evt Name of the event to create.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.defineEvent = function defineEvent(evt) {
        this.getListeners(evt);
        return this;
    };

    /**
     * Uses defineEvent to define multiple events.
     *
     * @param {String[]} evts An array of event names to define.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.defineEvents = function defineEvents(evts) {
        for (var i = 0; i < evts.length; i += 1) {
            this.defineEvent(evts[i]);
        }
        return this;
    };

    /**
     * Removes a listener function from the specified event.
     * When passed a regular expression as the event name, it will remove the listener from all events that match it.
     *
     * @param {String|RegExp} evt Name of the event to remove the listener from.
     * @param {Function} listener Method to remove from the event.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.removeListener = function removeListener(evt, listener) {
        var listeners = this.getListenersAsObject(evt);
        var index;
        var key;

        for (key in listeners) {
            if (listeners.hasOwnProperty(key)) {
                index = indexOfListener(listeners[key], listener);

                if (index !== -1) {
                    listeners[key].splice(index, 1);
                }
            }
        }

        return this;
    };

    /**
     * Alias of removeListener
     */
    proto.off = alias('removeListener');

    /**
     * Adds listeners in bulk using the manipulateListeners method.
     * If you pass an object as the first argument you can add to multiple events at once. The object should contain key value pairs of events and listeners or listener arrays. You can also pass it an event name and an array of listeners to be added.
     * You can also pass it a regular expression to add the array of listeners to all events that match it.
     * Yeah, this function does quite a bit. That's probably a bad thing.
     *
     * @param {String|Object|RegExp} evt An event name if you will pass an array of listeners next. An object if you wish to add to multiple events at once.
     * @param {Function[]} [listeners] An optional array of listener functions to add.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.addListeners = function addListeners(evt, listeners) {
        // Pass through to manipulateListeners
        return this.manipulateListeners(false, evt, listeners);
    };

    /**
     * Removes listeners in bulk using the manipulateListeners method.
     * If you pass an object as the first argument you can remove from multiple events at once. The object should contain key value pairs of events and listeners or listener arrays.
     * You can also pass it an event name and an array of listeners to be removed.
     * You can also pass it a regular expression to remove the listeners from all events that match it.
     *
     * @param {String|Object|RegExp} evt An event name if you will pass an array of listeners next. An object if you wish to remove from multiple events at once.
     * @param {Function[]} [listeners] An optional array of listener functions to remove.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.removeListeners = function removeListeners(evt, listeners) {
        // Pass through to manipulateListeners
        return this.manipulateListeners(true, evt, listeners);
    };

    /**
     * Edits listeners in bulk. The addListeners and removeListeners methods both use this to do their job. You should really use those instead, this is a little lower level.
     * The first argument will determine if the listeners are removed (true) or added (false).
     * If you pass an object as the second argument you can add/remove from multiple events at once. The object should contain key value pairs of events and listeners or listener arrays.
     * You can also pass it an event name and an array of listeners to be added/removed.
     * You can also pass it a regular expression to manipulate the listeners of all events that match it.
     *
     * @param {Boolean} remove True if you want to remove listeners, false if you want to add.
     * @param {String|Object|RegExp} evt An event name if you will pass an array of listeners next. An object if you wish to add/remove from multiple events at once.
     * @param {Function[]} [listeners] An optional array of listener functions to add/remove.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.manipulateListeners = function manipulateListeners(remove, evt, listeners) {
        var i;
        var value;
        var single = remove ? this.removeListener : this.addListener;
        var multiple = remove ? this.removeListeners : this.addListeners;

        // If evt is an object then pass each of its properties to this method
        if (typeof evt === 'object' && !(evt instanceof RegExp)) {
            for (i in evt) {
                if (evt.hasOwnProperty(i) && (value = evt[i])) {
                    // Pass the single listener straight through to the singular method
                    if (typeof value === 'function') {
                        single.call(this, i, value);
                    }
                    else {
                        // Otherwise pass back to the multiple function
                        multiple.call(this, i, value);
                    }
                }
            }
        }
        else {
            // So evt must be a string
            // And listeners must be an array of listeners
            // Loop over it and pass each one to the multiple method
            i = listeners.length;
            while (i--) {
                single.call(this, evt, listeners[i]);
            }
        }

        return this;
    };

    /**
     * Removes all listeners from a specified event.
     * If you do not specify an event then all listeners will be removed.
     * That means every event will be emptied.
     * You can also pass a regex to remove all events that match it.
     *
     * @param {String|RegExp} [evt] Optional name of the event to remove all listeners for. Will remove from every event if not passed.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.removeEvent = function removeEvent(evt) {
        var type = typeof evt;
        var events = this._getEvents();
        var key;

        // Remove different things depending on the state of evt
        if (type === 'string') {
            // Remove all listeners for the specified event
            delete events[evt];
        }
        else if (evt instanceof RegExp) {
            // Remove all events matching the regex.
            for (key in events) {
                if (events.hasOwnProperty(key) && evt.test(key)) {
                    delete events[key];
                }
            }
        }
        else {
            // Remove all listeners in all events
            delete this._events;
        }

        return this;
    };

    /**
     * Alias of removeEvent.
     *
     * Added to mirror the node API.
     */
    proto.removeAllListeners = alias('removeEvent');

    /**
     * Emits an event of your choice.
     * When emitted, every listener attached to that event will be executed.
     * If you pass the optional argument array then those arguments will be passed to every listener upon execution.
     * Because it uses `apply`, your array of arguments will be passed as if you wrote them out separately.
     * So they will not arrive within the array on the other side, they will be separate.
     * You can also pass a regular expression to emit to all events that match it.
     *
     * @param {String|RegExp} evt Name of the event to emit and execute listeners for.
     * @param {Array} [args] Optional array of arguments to be passed to each listener.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.emitEvent = function emitEvent(evt, args) {
        var listenersMap = this.getListenersAsObject(evt);
        var listeners;
        var listener;
        var i;
        var key;
        var response;

        for (key in listenersMap) {
            if (listenersMap.hasOwnProperty(key)) {
                listeners = listenersMap[key].slice(0);

                for (i = 0; i < listeners.length; i++) {
                    // If the listener returns true then it shall be removed from the event
                    // The function is executed either with a basic call or an apply if there is an args array
                    listener = listeners[i];

                    if (listener.once === true) {
                        this.removeListener(evt, listener.listener);
                    }

                    response = listener.listener.apply(this, args || []);

                    if (response === this._getOnceReturnValue()) {
                        this.removeListener(evt, listener.listener);
                    }
                }
            }
        }

        return this;
    };

    /**
     * Alias of emitEvent
     */
    proto.trigger = alias('emitEvent');

    /**
     * Subtly different from emitEvent in that it will pass its arguments on to the listeners, as opposed to taking a single array of arguments to pass on.
     * As with emitEvent, you can pass a regex in place of the event name to emit to all events that match it.
     *
     * @param {String|RegExp} evt Name of the event to emit and execute listeners for.
     * @param {...*} Optional additional arguments to be passed to each listener.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.emit = function emit(evt) {
        var args = Array.prototype.slice.call(arguments, 1);
        return this.emitEvent(evt, args);
    };

    /**
     * Sets the current value to check against when executing listeners. If a
     * listeners return value matches the one set here then it will be removed
     * after execution. This value defaults to true.
     *
     * @param {*} value The new value to check for when executing listeners.
     * @return {Object} Current instance of EventEmitter for chaining.
     */
    proto.setOnceReturnValue = function setOnceReturnValue(value) {
        this._onceReturnValue = value;
        return this;
    };

    /**
     * Fetches the current value to check against when executing listeners. If
     * the listeners return value matches this one then it should be removed
     * automatically. It will return true by default.
     *
     * @return {*|Boolean} The current value to check for or the default, true.
     * @api private
     */
    proto._getOnceReturnValue = function _getOnceReturnValue() {
        if (this.hasOwnProperty('_onceReturnValue')) {
            return this._onceReturnValue;
        }
        else {
            return true;
        }
    };

    /**
     * Fetches the events object and creates one if required.
     *
     * @return {Object} The events storage object.
     * @api private
     */
    proto._getEvents = function _getEvents() {
        return this._events || (this._events = {});
    };

    /**
     * Reverts the global {@link EventEmitter} to its previous value and returns a reference to this version.
     *
     * @return {Function} Non conflicting EventEmitter class.
     */
    EventEmitter.noConflict = function noConflict() {
        exports.EventEmitter = originalGlobalValue;
        return EventEmitter;
    };

    // Expose the class either via AMD, CommonJS or the global object
    if (true) {
        !(__WEBPACK_AMD_DEFINE_RESULT__ = (function () {
            return EventEmitter;
        }).call(exports, __webpack_require__, exports, module),
				__WEBPACK_AMD_DEFINE_RESULT__ !== undefined && (module.exports = __WEBPACK_AMD_DEFINE_RESULT__));
    }
    else {}
}(typeof window !== 'undefined' ? window : this || {}));


/***/ }),
/* 1 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);

// EXTERNAL MODULE: ./node_modules/wolfy87-eventemitter/EventEmitter.js
var EventEmitter = __webpack_require__(0);
var EventEmitter_default = /*#__PURE__*/__webpack_require__.n(EventEmitter);

// CONCATENATED MODULE: ./src/utils.ts
var __read = (undefined && undefined.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spread = (undefined && undefined.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read(arguments[i]));
    return ar;
};
/**
 * 可对小数进行“取模”的方法
 * @returns 返回(a / b)的余数
 */
function decableMod(a, b) {
    var n = Math.floor(a / b);
    return a - (n * b);
}
function loopedNumber(val, min, max) {
    if (min === max) {
        return min;
    }
    if (val < min) {
        // return val + max - min
        // max - (min - val) % (max - min)
        return max - decableMod(min - val, max - min);
    }
    if (val > max) {
        // return val + min - max
        // min + (val - max) % (max - min)
        return min + decableMod(val - max, max - min);
    }
    return val;
}
function clamp(val, val1, val2) {
    return (Math.max(val1, Math.min(val, val2)));
}
function fetchImg(url) {
    return new Promise(function (resolve, reject) {
        var img = new Image();
        function onload() {
            img.removeEventListener("load", onload);
            // eslint-disable-next-line @typescript-eslint/no-use-before-define
            img.removeEventListener("error", onerror);
            resolve(img);
        }
        function onerror(e) {
            img.removeEventListener("load", onload);
            img.removeEventListener("error", onerror);
            reject(e);
        }
        img.addEventListener("load", onload);
        img.addEventListener("error", onerror);
        img.src = url;
    });
}
function fetchImgs(inputUrls, totalProcessNum, itemDone) {
    if (totalProcessNum === void 0) { totalProcessNum = 4; }
    return new Promise(function (resolve) {
        var urls = __spread(inputUrls);
        var total = urls.length;
        var imgs = new Array(total).map(function () { return new Image(); });
        var curProcessNum = 0;
        var curIdx = 0;
        var loadedIdx = 0;
        function fetchInPool() {
            if (curProcessNum < totalProcessNum && urls.length > 0) {
                curIdx += 1;
                var _curIdx_1 = curIdx;
                var url = urls.shift();
                if (!url) {
                    return;
                }
                curProcessNum += 1;
                fetchImg(url)
                    .then(function (img) {
                    imgs[_curIdx_1] = img;
                })
                    .finally(function () {
                    loadedIdx += 1;
                    curProcessNum -= 1;
                    if (itemDone instanceof Function) {
                        itemDone(loadedIdx, total);
                    }
                    if (loadedIdx === total) {
                        resolve(imgs);
                    }
                    fetchInPool();
                });
            }
        }
        Array.from(new Array(totalProcessNum)).forEach(function () {
            fetchInPool();
        });
    });
}
function fetchDLImages(inputUrlArrayList, totalProcessNum, itemDone) {
    if (totalProcessNum === void 0) { totalProcessNum = 4; }
    return new Promise(function (resolve) {
        var urlArrayList = JSON.parse(JSON.stringify(inputUrlArrayList));
        var imgArrayList = inputUrlArrayList.map(function (urlArray) { return new Array(urlArray.length).map(function () { return new Image(); }); });
        var curProcessNum = 0;
        var x = 0;
        var y = 0;
        var nextIdx = 0;
        var loadedIdx = 0;
        var total = urlArrayList.reduce(function (prev, cur) { return prev + cur.length; }, 0);
        function fetchInPool() {
            if (curProcessNum < totalProcessNum && nextIdx < total) {
                var url = urlArrayList[x][y];
                var _x_1 = x;
                var _y_1 = y;
                if (y < urlArrayList[x].length - 1) {
                    y += 1;
                }
                else {
                    x += 1;
                    y = 0;
                }
                nextIdx += 1;
                if (!url) {
                    return;
                }
                curProcessNum += 1;
                fetchImg(url)
                    .then(function (img) {
                    imgArrayList[_x_1][_y_1] = img;
                })
                    .finally(function () {
                    curProcessNum -= 1;
                    loadedIdx += 1;
                    if (itemDone instanceof Function) {
                        itemDone(loadedIdx, total);
                    }
                    if (loadedIdx === total) {
                        resolve(imgArrayList);
                    }
                    fetchInPool();
                });
            }
        }
        Array.from(new Array(totalProcessNum)).forEach(function () {
            fetchInPool();
        });
    });
}
function scaleTo(x, y, targetX, targetY) {
    if (x / y < targetX / targetY) {
        return [
            x * (targetY / y),
            targetY,
        ];
    }
    return [
        targetX,
        y * (targetX / x),
    ];
}

// CONCATENATED MODULE: ./src/Vector2.ts
/**
 * @author mrdoob / http://mrdoob.com/
 * @author philogb / http://blog.thejit.org/
 * @author egraether / http://egraether.com/
 * @author zz85 / http://www.lab4games.net/zz85/blog
 */
var Vector2 = /** @class */ (function () {
    function Vector2(x, y) {
        this.x = 0;
        this.y = 0;
        this.isVector2 = true;
        this.x = x || 0;
        this.y = y || 0;
    }
    Object.defineProperty(Vector2.prototype, "width", {
        get: function () {
            return this.x;
        },
        set: function (value) {
            this.x = value;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(Vector2.prototype, "height", {
        get: function () {
            return this.y;
        },
        set: function (value) {
            this.y = value;
        },
        enumerable: true,
        configurable: true
    });
    Vector2.prototype.set = function (x, y) {
        this.x = x;
        this.y = y;
        return this;
    };
    Vector2.prototype.setScalar = function (scalar) {
        this.x = scalar;
        this.y = scalar;
        return this;
    };
    Vector2.prototype.setX = function (x) {
        this.x = x;
        return this;
    };
    Vector2.prototype.setY = function (y) {
        this.y = y;
        return this;
    };
    Vector2.prototype.setComponent = function (index, value) {
        switch (index) {
            case 0:
                this.x = value;
                break;
            case 1:
                this.y = value;
                break;
            default: throw new Error("index is out of range: " + index);
        }
        return this;
    };
    Vector2.prototype.getComponent = function (index) {
        switch (index) {
            case 0: return this.x;
            case 1: return this.y;
            default: throw new Error("index is out of range: " + index);
        }
    };
    Vector2.prototype.clone = function () {
        return new Vector2(this.x, this.y);
    };
    Vector2.prototype.copy = function (v) {
        this.x = v.x;
        this.y = v.y;
        return this;
    };
    Vector2.prototype.add = function (v) {
        this.x += v.x;
        this.y += v.y;
        return this;
    };
    Vector2.prototype.addScalar = function (s) {
        this.x += s;
        this.y += s;
        return this;
    };
    Vector2.prototype.addVectors = function (a, b) {
        this.x = a.x + b.x;
        this.y = a.y + b.y;
        return this;
    };
    Vector2.prototype.addScaledVector = function (v, s) {
        this.x += v.x * s;
        this.y += v.y * s;
        return this;
    };
    Vector2.prototype.sub = function (v) {
        this.x -= v.x;
        this.y -= v.y;
        return this;
    };
    Vector2.prototype.subScalar = function (s) {
        this.x -= s;
        this.y -= s;
        return this;
    };
    Vector2.prototype.subVectors = function (a, b) {
        this.x = a.x - b.x;
        this.y = a.y - b.y;
        return this;
    };
    Vector2.prototype.multiply = function (v) {
        this.x *= v.x;
        this.y *= v.y;
        return this;
    };
    Vector2.prototype.multiplyScalar = function (scalar) {
        this.x *= scalar;
        this.y *= scalar;
        return this;
    };
    Vector2.prototype.divide = function (v) {
        this.x /= v.x;
        this.y /= v.y;
        return this;
    };
    Vector2.prototype.divideScalar = function (scalar) {
        return this.multiplyScalar(1 / scalar);
    };
    Vector2.prototype.min = function (v) {
        this.x = Math.min(this.x, v.x);
        this.y = Math.min(this.y, v.y);
        return this;
    };
    Vector2.prototype.max = function (v) {
        this.x = Math.max(this.x, v.x);
        this.y = Math.max(this.y, v.y);
        return this;
    };
    Vector2.prototype.clamp = function (min, max) {
        // assumes min < max, componentwise
        this.x = Math.max(min.x, Math.min(max.x, this.x));
        this.y = Math.max(min.y, Math.min(max.y, this.y));
        return this;
    };
    Vector2.prototype.clampScalar = function (minVal, maxVal) {
        this.x = Math.max(minVal, Math.min(maxVal, this.x));
        this.y = Math.max(minVal, Math.min(maxVal, this.y));
        return this;
    };
    Vector2.prototype.clampLength = function (min, max) {
        var length = this.length();
        return this.divideScalar(length || 1).multiplyScalar(Math.max(min, Math.min(max, length)));
    };
    Vector2.prototype.floor = function () {
        this.x = Math.floor(this.x);
        this.y = Math.floor(this.y);
        return this;
    };
    Vector2.prototype.ceil = function () {
        this.x = Math.ceil(this.x);
        this.y = Math.ceil(this.y);
        return this;
    };
    Vector2.prototype.round = function () {
        this.x = Math.round(this.x);
        this.y = Math.round(this.y);
        return this;
    };
    Vector2.prototype.roundToZero = function () {
        this.x = (this.x < 0) ? Math.ceil(this.x) : Math.floor(this.x);
        this.y = (this.y < 0) ? Math.ceil(this.y) : Math.floor(this.y);
        return this;
    };
    Vector2.prototype.negate = function () {
        this.x = -this.x;
        this.y = -this.y;
        return this;
    };
    Vector2.prototype.dot = function (v) {
        return this.x * v.x + this.y * v.y;
    };
    Vector2.prototype.cross = function (v) {
        return this.x * v.y - this.y * v.x;
    };
    Vector2.prototype.lengthSq = function () {
        return this.x * this.x + this.y * this.y;
    };
    Vector2.prototype.length = function () {
        return Math.sqrt(this.x * this.x + this.y * this.y);
    };
    Vector2.prototype.manhattanLength = function () {
        return Math.abs(this.x) + Math.abs(this.y);
    };
    Vector2.prototype.normalize = function () {
        return this.divideScalar(this.length() || 1);
    };
    Vector2.prototype.angle = function () {
        // computes the angle in radians with respect to the positive x-axis
        var angle = Math.atan2(this.y, this.x);
        if (angle < 0)
            angle += 2 * Math.PI;
        return angle;
    };
    Vector2.prototype.distanceTo = function (v) {
        return Math.sqrt(this.distanceToSquared(v));
    };
    Vector2.prototype.distanceToSquared = function (v) {
        var dx = this.x - v.x;
        var dy = this.y - v.y;
        return dx * dx + dy * dy;
    };
    Vector2.prototype.manhattanDistanceTo = function (v) {
        return Math.abs(this.x - v.x) + Math.abs(this.y - v.y);
    };
    Vector2.prototype.setLength = function (length) {
        return this.normalize().multiplyScalar(length);
    };
    Vector2.prototype.lerp = function (v, alpha) {
        this.x += (v.x - this.x) * alpha;
        this.y += (v.y - this.y) * alpha;
        return this;
    };
    Vector2.prototype.lerpVectors = function (v1, v2, alpha) {
        return this.subVectors(v2, v1).multiplyScalar(alpha).add(v1);
    };
    Vector2.prototype.equals = function (v) {
        return ((v.x === this.x) && (v.y === this.y));
    };
    Vector2.prototype.fromArray = function (array, offset) {
        var realOffset = offset || 0;
        this.x = array[realOffset];
        this.y = array[realOffset + 1];
        return this;
    };
    Vector2.prototype.toArray = function (array, offset) {
        var realArray = array || [];
        var realOffset = offset || 0;
        realArray[realOffset] = this.x;
        realArray[realOffset + 1] = this.y;
        return realArray;
    };
    Vector2.prototype.rotateAround = function (center, angle) {
        var c = Math.cos(angle);
        var s = Math.sin(angle);
        var x = this.x - center.x;
        var y = this.y - center.y;
        this.x = x * c - y * s + center.x;
        this.y = x * s + y * c + center.y;
        return this;
    };
    return Vector2;
}());
/* harmony default export */ var src_Vector2 = (Vector2);

// CONCATENATED MODULE: ./src/InteractiveElement.ts
var __extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
/* eslint-disable no-bitwise */



var EPS = 0.000001;
var InteractiveElement_InteractiveElement = /** @class */ (function (_super) {
    __extends(InteractiveElement, _super);
    function InteractiveElement(domElement) {
        var _this = _super.call(this) || this;
        _this.enabled = true;
        _this.enableLooped = true;
        _this.enableRotate = true;
        _this.enableRotateDamping = true;
        _this.enableScale = true;
        _this.enableScaleDamping = true;
        /**
         * 手指滑过一个[this.domSizeVectors.x]时，h(或v)的变化量
         */
        _this.rotateSpeed = 120;
        /**
         * 手指缩放一个[this.domSizeVectors.x]时，fov的变化量
         */
        _this.scaleSpeed = -100;
        /**
         * 水平方向倒转
         * 该参数为 true 时将忽略 enableLooped
         * 示例: 如果 h 限制为 [-75, 75], 倒转后变成 [-180, -75] && [75, 180]
         */
        _this.horizonReversed = false;
        /**
         * 每个代码执行周期内 自动旋转的角度
         * 该值乘以fps(一般为60)即为每秒旋转的角度
         */
        _this.autoRotateSpeed = -0.05;
        /**
         * 启动自动旋转的时间间隔
         */
        _this.autoRotateInterval = 15000;
        /**
         * h/v发生设置行为(不论是否变动)时触发
         */
        _this.ROTATE_EVENT = "rotate";
        /**
         * fov发生设置行为(不论是否变动)时触发
         */
        _this.SCALE_EVENT = "scale";
        /**
         * updateCamera时触发
         */
        _this.CHANGE_EVENT = "change";
        /**
         * 用户交互时触发
         */
        _this.INTERACT_EVENT = "interact";
        /**
         * 一个尺寸的量，触摸旋转时以此为基准
         * 该值越大，则旋转同样角度，手指需要滑动的距离越大
         */
        _this.domSizeVectors = new src_Vector2();
        _this._scaleCenter = new src_Vector2();
        _this._centerCoords = new src_Vector2();
        _this._STATES = {
            NONE: 0,
            ROTATE: 1,
            SCALE: 1 << 1,
        };
        _this._state = _this._STATES.NONE;
        _this._h = 0;
        _this._v = 90;
        _this._fov = 90;
        _this._minFov = 40;
        _this._maxFov = 140;
        _this._minV = EPS;
        _this._maxV = 180 - EPS;
        _this._minH = -180;
        _this._maxH = 180;
        _this._rotateSmoothFactor = 0.9;
        _this._scaleSmoothFactor = 0.9;
        _this._enableAutoRotate = false;
        _this._autoRotateStart = new Date().getTime();
        _this.touchScaleStart = 0;
        _this.scaleDelta = 0;
        _this.touchScaleEnd = 0;
        _this.rotateStart = new src_Vector2();
        _this.rotateDelta = new src_Vector2();
        _this.rotateEnd = new src_Vector2();
        _this.setCursor = function (cursorStr) {
            _this.domElement.style.cursor = cursorStr;
        };
        /**
         * resize的时候重置domSizeVectors
         */
        _this.onResize = function () {
            var _a = _this.domElement, clientWidth = _a.clientWidth, clientHeight = _a.clientHeight;
            var greatVal = clientWidth > clientHeight
                ? clientWidth
                : clientHeight;
            _this.domSizeVectors.set(greatVal, greatVal);
        };
        _this.onTouchStart = function (e) {
            // touchstart可以阻止浏览器默认右滑行为
            e.preventDefault();
            var touches = e.touches;
            switch (touches.length) {
                case 1:
                    _this.onRotateStart(touches[0], true);
                    break;
                case 2:
                    _this.onTouchScaleStart(touches);
                    break;
                default:
                    break;
            }
        };
        _this.onTouchMove = function (e) {
            e.preventDefault();
            var touches = e.touches;
            switch (touches.length) {
                case 1:
                    _this.onRotate(touches[0]);
                    break;
                case 2:
                    _this.onTouchScale(touches);
                    break;
                default:
                    break;
            }
        };
        _this.onTouchEnd = function (e) {
            e.preventDefault();
            var totalTouchesLength = e.touches.length + e.changedTouches.length;
            switch (totalTouchesLength) {
                case 1:
                    _this.onRotateEnd(e, true);
                    break;
                case 2:
                    _this.onTouchScaleEnd();
                    break;
                default:
                    break;
            }
        };
        _this.onMouseDown = function (e) {
            _this.setCursor("grabbing");
            e.preventDefault();
            _this.onRotateStart(e);
        };
        _this.onMouseMove = function (e) {
            e.preventDefault();
            _this.onRotate(e);
        };
        _this.onMouseUp = function (e) {
            _this.setCursor("grab");
            e.preventDefault();
            _this.onRotateEnd(e);
        };
        _this.onWheel = function (e) {
            e.preventDefault();
            _this.onMouseScale(e);
        };
        _this.domElement = domElement;
        _this._rect = _this.domElement.getBoundingClientRect();
        _this.setCursor("grab");
        _this.onResize();
        _this.addEvents();
        return _this;
    }
    Object.defineProperty(InteractiveElement.prototype, "scaleCenter", {
        get: function () {
            this._scaleCenter.set(this._centerCoords.x - this._rect.left, this._centerCoords.y - this._rect.top);
            return this._scaleCenter;
        },
        enumerable: true,
        configurable: true
    });
    InteractiveElement.prototype.reinit = function (domElement) {
        this.removeEvents();
        this.domElement = domElement;
        this._rect = this.domElement.getBoundingClientRect();
        this.fov = this.maxFov;
        this._state = this._STATES.NONE;
        this.touchScaleStart = 0;
        this.scaleDelta = 0;
        this.touchScaleEnd = 0;
        this.rotateStart = new src_Vector2();
        this.rotateDelta = new src_Vector2();
        this.rotateEnd = new src_Vector2();
        this.domSizeVectors = new src_Vector2();
        this._centerCoords = new src_Vector2();
        this.resetAutoRotateTimer();
        this.onResize();
        this.addEvents();
    };
    InteractiveElement.prototype.updateRect = function () {
        this._rect = this.domElement.getBoundingClientRect();
    };
    InteractiveElement.prototype.resetAutoRotateTimer = function () {
        this._autoRotateStart = new Date().getTime();
    };
    InteractiveElement.prototype.onRotateStart = function (_a, isTouch) {
        var clientX = _a.clientX, clientY = _a.clientY;
        if (isTouch === void 0) { isTouch = false; }
        this._state |= this._STATES.ROTATE;
        this.rotateStart.set(clientX, clientY);
        this.rotateEnd.set(clientX, clientY);
        if (isTouch) {
            document.addEventListener("touchmove", this.onTouchMove);
            document.addEventListener("touchend", this.onTouchEnd);
        }
        else {
            document.addEventListener("mousemove", this.onMouseMove);
            document.addEventListener("mouseup", this.onMouseUp);
        }
    };
    InteractiveElement.prototype.onRotate = function (_a) {
        var clientX = _a.clientX, clientY = _a.clientY;
        this.rotateEnd.set(clientX, clientY);
        this.update();
        this.rotateStart.set(clientX, clientY);
        this.emitEvent(this.INTERACT_EVENT, []);
        if (this.enableAutoRotate) {
            this.resetAutoRotateTimer();
        }
    };
    InteractiveElement.prototype.onRotateEnd = function (e, isTouch) {
        if (isTouch === void 0) { isTouch = false; }
        this._state &= ~this._STATES.ROTATE;
        if (isTouch) {
            document.removeEventListener("touchmove", this.onTouchMove);
            document.removeEventListener("touchend", this.onTouchEnd);
        }
        else {
            document.removeEventListener("mousemove", this.onMouseMove);
            document.removeEventListener("mouseup", this.onMouseUp);
        }
    };
    InteractiveElement.prototype.onTouchScaleStart = function (touches) {
        this._state |= this._STATES.SCALE;
        var _a = touches[0], x1 = _a.clientX, y1 = _a.clientY;
        var _b = touches[1], x2 = _b.clientX, y2 = _b.clientY;
        this.touchScaleStart = new src_Vector2(x1, y1).distanceTo(new src_Vector2(x2, y2)) / this.domSizeVectors.x;
        this.touchScaleEnd = this.touchScaleStart;
        this._centerCoords.set((x1 + x2) / 2, (y1 + y2) / 2);
        document.addEventListener("touchmove", this.onTouchMove);
        document.addEventListener("touchend", this.onTouchEnd);
    };
    InteractiveElement.prototype.onTouchScale = function (touches) {
        var _a = touches[0], x1 = _a.clientX, y1 = _a.clientY;
        var _b = touches[1], x2 = _b.clientX, y2 = _b.clientY;
        this.touchScaleEnd = new src_Vector2(x1, y1).distanceTo(new src_Vector2(x2, y2)) / this.domSizeVectors.x;
        this.update();
        this.touchScaleStart = this.touchScaleEnd;
        this.emitEvent(this.INTERACT_EVENT, []);
        if (this.enableAutoRotate) {
            this.resetAutoRotateTimer();
        }
    };
    InteractiveElement.prototype.onTouchScaleEnd = function () {
        this._state &= ~this._STATES.SCALE;
        this.touchScaleStart = 0;
        this.touchScaleEnd = 0;
        document.removeEventListener("touchmove", this.onTouchMove);
        document.removeEventListener("touchend", this.onTouchEnd);
    };
    InteractiveElement.prototype.onMouseScale = function (e) {
        var deltaY = e.deltaY;
        var clientX = e.clientX, clientY = e.clientY;
        this._centerCoords.set(clientX, clientY);
        if (Math.abs(deltaY) < 10) {
            /**
             * 懒得写各种兼容了，发现浏览器实现得千奇百怪，往后滚动滚轮一格，deltaY值有以下实现：
             * 以下浏览器均为win10下的64位浏览器：
             * chrome 72.0.3626.121: 100
             * Firefox Quantum 66.0.2: 3
             * ie 11.503.17763.0 最令人发指，会随着页面高度动态改变，从50变到200+...
             */
            // eslint-disable-next-line no-param-reassign
            deltaY *= 40;
        }
        this._state |= this._STATES.SCALE;
        this.touchScaleStart = 0;
        /**
         * WARNING!!!
         * 这里除以7000，是经验值，表示鼠标滚轮和触摸屏之间的协调参数
         */
        this.touchScaleEnd = -deltaY / 7000;
        this.update();
        this._state &= ~this._STATES.SCALE;
        this.emitEvent(this.INTERACT_EVENT, []);
        if (this.enableAutoRotate) {
            this.resetAutoRotateTimer();
        }
    };
    InteractiveElement.prototype.addEvents = function () {
        this.domElement.addEventListener("mousedown", this.onMouseDown);
        this.domElement.addEventListener("touchstart", this.onTouchStart);
        this.domElement.addEventListener("wheel", this.onWheel);
        window.addEventListener("resize", this.onResize);
    };
    InteractiveElement.prototype.removeEvents = function () {
        this.domElement.removeEventListener("mousedown", this.onMouseDown);
        this.domElement.removeEventListener("touchstart", this.onTouchStart);
        this.domElement.removeEventListener("wheel", this.onWheel);
        window.removeEventListener("resize", this.onResize);
        document.removeEventListener("touchmove", this.onTouchMove);
        document.removeEventListener("touchend", this.onTouchEnd);
        document.removeEventListener("mousemove", this.onMouseMove);
        document.removeEventListener("mouseup", this.onMouseUp);
    };
    InteractiveElement.prototype.update = function () {
        if (!this.enabled) {
            return;
        }
        var rotateNeedUpdate = this.enableRotate && this.updateRotateDelta();
        var scaleNeedUpdate = this.enableScale && this.updateScaleDelta();
        if (rotateNeedUpdate || scaleNeedUpdate) {
            this.updateCamera();
        }
    };
    InteractiveElement.prototype.updateCamera = function () {
        this.emitEvent(this.CHANGE_EVENT, []);
    };
    InteractiveElement.prototype.updateRotateDelta = function () {
        var needsUpdate = false;
        if (this._state & this._STATES.ROTATE) {
            this.rotateDelta
                .subVectors(this.rotateEnd, this.rotateStart)
                .divide(this.domSizeVectors)
                .multiplyScalar(this.rotateSpeed);
            needsUpdate = true;
        }
        else if (this.enableRotateDamping && this.rotateDelta.lengthSq() > EPS) {
            this.rotateDelta.multiplyScalar(this.rotateSmoothFactor);
            needsUpdate = true;
        }
        else {
            this.rotateDelta.set(0, 0);
        }
        var delta = new Date().getTime() - this._autoRotateStart;
        if (this.enableAutoRotate && (delta > this.autoRotateInterval)) {
            this._state |= this._STATES.ROTATE;
            this.rotateDelta.x += this.autoRotateSpeed;
            needsUpdate = true;
        }
        if (needsUpdate) {
            this.h += this.rotateDelta.x;
            this.v -= this.rotateDelta.y;
        }
        return needsUpdate;
    };
    InteractiveElement.prototype.updateScaleDelta = function () {
        var needsUpdate = false;
        if (this._state & this._STATES.SCALE) {
            this.scaleDelta = (this.touchScaleEnd - this.touchScaleStart) * this.scaleSpeed;
            this.fov += this.scaleDelta;
            needsUpdate = true;
        }
        else if (this.enableScaleDamping && Math.abs(this.scaleDelta) > EPS) {
            this.scaleDelta *= this.scaleSmoothFactor;
            this.fov += this.scaleDelta;
            needsUpdate = true;
        }
        else {
            this.scaleDelta = 0;
        }
        return needsUpdate;
    };
    Object.defineProperty(InteractiveElement.prototype, "enableAutoRotate", {
        get: function () {
            return this._enableAutoRotate;
        },
        set: function (val) {
            this._enableAutoRotate = val;
            if (val) {
                this.resetAutoRotateTimer();
            }
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "rotateSmoothFactor", {
        /**
         * 旋转衰减因子，between 0 and 1
         */
        get: function () {
            return this._rotateSmoothFactor;
        },
        set: function (val) {
            if (val <= 0 || val >= 1) {
                console.error("PanoControls.rotateSmoothFactor must be a value between 0 and 1.");
            }
            this._rotateSmoothFactor = clamp(val, EPS, 1 - EPS);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "scaleSmoothFactor", {
        get: function () {
            return this._scaleSmoothFactor;
        },
        /**
         * 缩放衰减因子，between 0 and 1
         */
        set: function (val) {
            if (val <= 0 || val >= 1) {
                console.error("PanoControls.scaleSmoothFactor must be a value between 0 and 1.");
            }
            this._scaleSmoothFactor = clamp(val, EPS, 1 - EPS);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "h", {
        get: function () {
            return this._h;
        },
        set: function (val) {
            var realVal = val;
            if (this.horizonReversed) {
                if (realVal < -180) {
                    realVal = 180;
                }
                else if (realVal > 180) {
                    realVal = -180;
                }
                else if (realVal > this.minH && realVal < this.maxH) {
                    if (this.h <= this.minH) {
                        realVal = this.minH;
                    }
                    else {
                        realVal = this.maxH;
                    }
                }
            }
            else {
                realVal = this.enableLooped
                    ? loopedNumber(realVal, this.minH, this.maxH)
                    : clamp(realVal, this.minH, this.maxH);
            }
            this._h = realVal;
            this.emitEvent(this.ROTATE_EVENT, []);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "v", {
        get: function () {
            return this._v;
        },
        set: function (val) {
            var realVal = clamp(val, this.minV, this.maxV);
            this._v = realVal;
            this.emitEvent(this.ROTATE_EVENT, []);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "fov", {
        get: function () {
            return this._fov;
        },
        set: function (val) {
            this._fov = clamp(val, this.minFov, this.maxFov);
            this.emitEvent(this.SCALE_EVENT, []);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "minFov", {
        get: function () {
            return this._minFov;
        },
        set: function (val) {
            this._minFov = clamp(val, EPS, this.maxFov);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "maxFov", {
        get: function () {
            return this._maxFov;
        },
        set: function (val) {
            this._maxFov = clamp(val, this.minFov, 180 - EPS);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "minV", {
        get: function () {
            return this._minV;
        },
        set: function (val) {
            this._minV = clamp(val, EPS, this.maxV);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "maxV", {
        get: function () {
            return this._maxV;
        },
        set: function (val) {
            this._maxV = clamp(val, this.minV, 180 - EPS);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "minH", {
        get: function () {
            return this._minH;
        },
        set: function (val) {
            this._minH = clamp(val, -180, this.maxH);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(InteractiveElement.prototype, "maxH", {
        get: function () {
            return this._maxH;
        },
        set: function (val) {
            this._maxH = clamp(val, this.minH, 180);
        },
        enumerable: true,
        configurable: true
    });
    return InteractiveElement;
}(EventEmitter_default.a));
/* harmony default export */ var src_InteractiveElement = (InteractiveElement_InteractiveElement);

// CONCATENATED MODULE: ./src/index.ts
/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, "Pano", function() { return src_Pano; });
/* concated harmony reexport Vector2 */__webpack_require__.d(__webpack_exports__, "Vector2", function() { return src_Vector2; });
/* concated harmony reexport InteractiveElement */__webpack_require__.d(__webpack_exports__, "InteractiveElement", function() { return src_InteractiveElement; });
/* concated harmony reexport fetchImg */__webpack_require__.d(__webpack_exports__, "fetchImg", function() { return fetchImg; });
/* concated harmony reexport fetchImgs */__webpack_require__.d(__webpack_exports__, "fetchImgs", function() { return fetchImgs; });
/* concated harmony reexport fetchDLImages */__webpack_require__.d(__webpack_exports__, "fetchDLImages", function() { return fetchDLImages; });
/* concated harmony reexport scaleTo */__webpack_require__.d(__webpack_exports__, "scaleTo", function() { return scaleTo; });
var src_read = (undefined && undefined.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var src_spread = (undefined && undefined.__spread) || function () {
    for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(src_read(arguments[i]));
    return ar;
};
/* eslint-disable @typescript-eslint/camelcase */



console.log("Pano " + "1.6.0");
var src_Pano = /** @class */ (function () {
    /**
     * 更换参数时可以执行reinit方法
     * canvas的size发生改变时, 需执行this.resize方法
     * canvas的距离视口左上角的坐标发生改变时, 需执行this.interactiveElement.updateRect方法
     */
    function Pano(_a) {
        var _this = this;
        var urls = _a.urls, canvas = _a.canvas, _b = _a.keepImgRatio, keepImgRatio = _b === void 0 ? true : _b, onImgLoaded = _a.onImgLoaded;
        /**
         * 链接列表
         */
        this.urls = [[]];
        /**
         * 旋转时是否渲染其他图层
         * 关闭可大幅度提高程序的运行性能(但用户感官体验相对就下降了)
         */
        this.renderWhileRotate = true;
        this._imgs = [[]];
        this._imgSize = new src_Vector2();
        this._virtualImgCenter = new src_Vector2();
        this._virtualImgSize = new src_Vector2();
        this._keepImgRatio = true;
        this._fov = 90;
        this._isRotating = false;
        this._timer = 0;
        this._lastIdxVal = -1;
        this.urls = urls;
        this.canvas = canvas;
        this.ctx = canvas.getContext("2d");
        this._keepImgRatio = keepImgRatio;
        this.interactiveElement = new src_InteractiveElement(this.canvas);
        // 限制fov的范围
        this.interactiveElement.minFov = 40;
        this.interactiveElement.maxFov = this._fov;
        // 初始化最佳实践
        // this.interactiveElement.enableScaleDamping = false
        this.interactiveElement.rotateSmoothFactor = 0.9;
        this.interactiveElement.rotateSpeed = -200;
        this.interactiveElement.scaleSpeed = -140;
        this.interactiveElement.h = -180;
        this.interactiveElement.fov = this._fov;
        this.interactiveElement.updateCamera();
        // 加载第一张图片作为预览图
        fetchImg(this.urls[0][0]).then(function (img) {
            _this._setAsImgSize(img);
            _this._drawImage(img);
        });
        fetchDLImages(this.urls, 4, onImgLoaded).then(function (imgs) {
            var availableImg = imgs[0].find(function (img) { return (img.width > 0) && (img.height > 0); });
            _this._setAsImgSize(availableImg);
            _this._imgs = imgs;
            _this.update();
        });
    }
    Object.defineProperty(Pano.prototype, "_lastIdx", {
        get: function () {
            return this._lastIdxVal;
        },
        set: function (val) {
            var _this = this;
            if (val !== this._lastIdxVal) {
                if (this._timer) {
                    clearTimeout(this._timer);
                }
                this._isRotating = true;
                this._lastIdxVal = val;
                this._timer = window.setTimeout(function () {
                    _this._isRotating = false;
                }, 300);
            }
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(Pano.prototype, "_canvasSize", {
        get: function () {
            return new src_Vector2(this.canvas.width, this.canvas.height);
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(Pano.prototype, "_isFatImg", {
        get: function () {
            return this._imgSize.width / this._imgSize.height > this._canvasSize.width / this._canvasSize.height;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(Pano.prototype, "_scaleDelta", {
        get: function () {
            return this._imgSize.width / this._virtualImgSize.width;
        },
        enumerable: true,
        configurable: true
    });
    Object.defineProperty(Pano.prototype, "_params", {
        get: function () {
            var _a = this, _virtualImgCenter = _a._virtualImgCenter, _virtualImgSize = _a._virtualImgSize, _canvasSize = _a._canvasSize, _scaleDelta = _a._scaleDelta;
            var halfImgWidth = _virtualImgSize.width / 2;
            var halfImgHeight = _virtualImgSize.height / 2;
            var halfCanvasWidth = _canvasSize.width / 2;
            var halfCanvasHeight = _canvasSize.height / 2;
            var cx = _virtualImgCenter.x, cy = _virtualImgCenter.y;
            var sx = 0;
            var sy = 0;
            var sw = 0;
            var sh = 0;
            var dx = 0;
            var dy = 0;
            var dw = 0;
            var dh = 0;
            if (cx) {
                if (cy > 0) {
                    sw = halfCanvasWidth + halfImgWidth - cx;
                    sh = halfCanvasHeight + halfImgHeight - cy;
                    dw = sw;
                    dh = sh;
                    sx = 0;
                    sy = 2 * halfImgHeight - sh;
                    dx = 2 * halfCanvasWidth - dw;
                    dy = 0;
                }
                else {
                    sw = halfCanvasWidth + halfImgWidth - cx;
                    sh = halfCanvasHeight + halfImgHeight + cy;
                    dw = sw;
                    dh = sh;
                    sx = 0;
                    sy = 0;
                    dx = 2 * halfCanvasWidth - dw;
                    dy = 2 * halfCanvasHeight - dh;
                }
            }
            else if (cy > 0) {
                dw = halfCanvasWidth + halfImgWidth + cx;
                dh = halfCanvasHeight + halfImgHeight - cy;
                sw = dw;
                sh = dh;
                sx = 2 * halfImgWidth - sw;
                sy = 2 * halfImgHeight - sh;
                dx = 0;
                dy = 0;
            }
            else {
                dw = halfCanvasWidth + halfImgWidth + cx;
                dh = halfCanvasHeight + halfImgHeight + cy;
                sw = dw;
                sh = dh;
                sx = 2 * halfImgWidth - sw;
                sy = 0;
                dx = 0;
                dy = 2 * halfCanvasHeight - dh;
            }
            var result = [
                sx * _scaleDelta,
                sy * _scaleDelta,
                sw * _scaleDelta,
                sh * _scaleDelta,
                dx,
                dy,
                dw,
                dh,
            ];
            return result;
        },
        enumerable: true,
        configurable: true
    });
    Pano.prototype.reinit = function (options) {
        var _this = this;
        var _a = {
            urls: options.urls || this.urls,
            canvas: options.canvas || this.canvas,
            keepImgRatio: options.keepImgRatio !== undefined ? options.keepImgRatio : this._keepImgRatio,
            onImgLoaded: options.onImgLoaded,
        }, urls = _a.urls, canvas = _a.canvas, keepImgRatio = _a.keepImgRatio, onImgLoaded = _a.onImgLoaded;
        this.urls = urls;
        this.canvas = canvas;
        this.ctx = canvas.getContext("2d");
        this._keepImgRatio = keepImgRatio;
        this.interactiveElement.reinit(this.canvas);
        this._fov = this.interactiveElement.fov;
        // 加载第一张图片作为预览图
        // 加载第一张图片作为预览图
        fetchImg(this.urls[0][0]).then(function (img) {
            _this._setAsImgSize(img);
            _this._drawImage(img);
        });
        fetchDLImages(this.urls, 4, onImgLoaded).then(function (imgs) {
            var availableImg = imgs[0].find(function (img) { return (img.width > 0) && (img.height > 0); });
            _this._setAsImgSize(availableImg);
            _this._imgs = imgs;
            _this.update();
        });
    };
    Pano.prototype._setAsImgSize = function (img) {
        this._imgSize.set(img.width, img.height);
        this.resize();
    };
    Pano.prototype.resize = function (doResizeCanvas) {
        if (doResizeCanvas === void 0) { doResizeCanvas = true; }
        if (doResizeCanvas) {
            this.resizeCanvas();
        }
        var maxFov = this.interactiveElement.maxFov;
        this._fov = maxFov;
        // 是否需要恢复成初始状态
        // this.interactiveElement.fov = fov
        this._virtualImgSize = new (src_Vector2.bind.apply(src_Vector2, src_spread([void 0], scaleTo(this._imgSize.width, this._imgSize.height, this._canvasSize.width, this._canvasSize.height))))();
        this._virtualImgCenter = new src_Vector2();
    };
    Pano.prototype.resizeCanvas = function (size) {
        var canvas = this.canvas;
        var width = size ? size.width : canvas.clientWidth;
        var height = size ? size.height : canvas.clientHeight;
        if (canvas.width !== width || canvas.height !== height) {
            this.canvas.width = width;
            this.canvas.height = height;
            return true;
        }
        return false;
    };
    Pano.prototype.drawPoint = function (v, r) {
        if (r === void 0) { r = 5; }
        var ctx = this.ctx;
        ctx.fillStyle = "rgb(200,0,0)";
        ctx.fillRect(v.x - r, v.y - r, 2 * r, 2 * r);
    };
    Pano.prototype._drawImage = function (img) {
        var _a;
        if (img) {
            (_a = this.ctx).drawImage.apply(_a, src_spread([img], this._params));
        }
        else {
            console.error("is not a valid image: ", img);
        }
    };
    Pano.prototype.update = function () {
        var _this = this;
        var imgNum = this._imgs[0].length;
        if (imgNum === 0) {
            return;
        }
        this.interactiveElement.update();
        var _a = this, _fov = _a._fov, interactiveElement = _a.interactiveElement, _canvasSize = _a._canvasSize;
        var width = _canvasSize.width, height = _canvasSize.height;
        var h = interactiveElement.h, fov = interactiveElement.fov, scaleCenter = interactiveElement.scaleCenter;
        var revertScaleCenter = new src_Vector2(scaleCenter.x - width / 2, height / 2 - scaleCenter.y);
        // DF > 1: 放大
        // DF < 1: 缩小
        var DF = _fov / fov;
        this._virtualImgSize.multiplyScalar(DF);
        if (DF < 1) { // 缩小时将 虚拟图片中心点 迅速向原点靠近
            var d = (this.interactiveElement.maxFov - fov) / (this.interactiveElement.maxFov - _fov);
            this._virtualImgCenter.multiplyScalar(d * d * d);
        }
        else {
            this._virtualImgCenter.copy(new src_Vector2().subVectors(this._virtualImgCenter, revertScaleCenter).multiplyScalar(DF).add(revertScaleCenter));
        }
        var y = Math.floor(((h + 180) / 360) * imgNum);
        this._lastIdx = y;
        this._fov = fov;
        if (this._imgs[0][y]) {
            this.ctx.clearRect(0, 0, width, height);
        }
        if (!this.renderWhileRotate && this._isRotating) { // 旋转时仅仅渲染第一张图
            this._drawImage(this._imgs[0][y]);
        }
        else {
            this._imgs.forEach(function (imgArray) {
                _this._drawImage(imgArray[y]);
            });
        }
        // this.drawPoint(new Vector2(
        //   this._virtualImgCenter.x + _canvasSize.width / 2,
        //   _canvasSize.height / 2 - this._virtualImgCenter.y,
        // ))
    };
    return Pano;
}());



/***/ })
/******/ ]);
});
//# sourceMappingURL=index.min.js.map

/***/ }),
/* 2 */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
__webpack_require__.r(__webpack_exports__);
/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(0);
/* harmony import */ var three__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(three__WEBPACK_IMPORTED_MODULE_0__);
/* harmony import */ var tang_pano__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(1);
/* harmony import */ var tang_pano__WEBPACK_IMPORTED_MODULE_1___default = /*#__PURE__*/__webpack_require__.n(tang_pano__WEBPACK_IMPORTED_MODULE_1__);
/* eslint-disable no-bitwise */
/* eslint-disable @typescript-eslint/ban-ts-ignore */
var __extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = function (d, b) {
        extendStatics = Object.setPrototypeOf ||
            ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
            function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
        return extendStatics(d, b);
    };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();


console.log("PanoControls " + "3.0.0");
var PI = Math.PI;
var PanoControls = /** @class */ (function (_super) {
    __extends(PanoControls, _super);
    function PanoControls(camera, domElement) {
        var _this = _super.call(this, domElement) || this;
        _this.target = new three__WEBPACK_IMPORTED_MODULE_0__["Vector3"]();
        _this.spherical = new three__WEBPACK_IMPORTED_MODULE_0__["Spherical"]();
        _this.camera = camera;
        var vec3 = camera.getWorldDirection(new three__WEBPACK_IMPORTED_MODULE_0__["Vector3"]());
        _this.target.copy(camera.position).add(vec3);
        _this.spherical.setFromVector3(vec3);
        _this.h = (_this.spherical.theta * 180) / PI;
        _this.v = (_this.spherical.phi * 180) / PI;
        _this.fov = _this.camera.fov;
        return _this;
    }
    PanoControls.prototype.update = function () {
        if (!this.enabled) {
            return;
        }
        var rotateNeedUpdate = this.enableRotate && this.updateRotateDelta();
        var scaleNeedUpdate = this.enableScale && this.updateScaleDelta();
        if (rotateNeedUpdate || scaleNeedUpdate) {
            this.updateCamera();
        }
    };
    PanoControls.prototype.updateCamera = function () {
        var _a = this, target = _a.target, spherical = _a.spherical, camera = _a.camera, h = _a.h, v = _a.v, fov = _a.fov;
        spherical.theta = (h / 180) * PI;
        spherical.phi = (v / 180) * PI;
        camera.fov = fov;
        target.setFromSpherical(spherical).add(camera.position);
        camera.lookAt(target);
        camera.updateProjectionMatrix();
        this.emitEvent(this.CHANGE_EVENT, []);
    };
    return PanoControls;
}(tang_pano__WEBPACK_IMPORTED_MODULE_1__["InteractiveElement"]));
/* harmony default export */ __webpack_exports__["default"] = (PanoControls);


/***/ })
/******/ ])["default"];
});
//# sourceMappingURL=index.min.js.map